# For local development only.
# On Railway, DATABASE_URL is injected automatically once you add a
# PostgreSQL plugin and reference it from this service's variables.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/railway_test_app
PORT=3001
