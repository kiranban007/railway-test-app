{
  "name": "railway-test-app",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && rm -rf backend/public && cp -r frontend/dist backend/public && npm install --prefix backend",
    "start": "node backend/server.js"
  }
}
